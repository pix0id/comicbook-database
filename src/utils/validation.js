const addComicValidation = data => {
    /**
     * TODO: validate comics, make sure required fields are allowed values/strings longer than 0 characters
     */
    return true
}


module.exports = {
    addComicValidation
}